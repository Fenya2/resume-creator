server:
  servlet:
    context-path: /app
    address: 0.0.0.0
    port: 8080
  tomcat:
    basedir: "tomcat"
    accesslog:
      enabled: true
spring:
  thymeleaf:
    encoding: UTF-8
    mode: HTML
  datasource:
    url: jdbc:postgresql://localhost:5432/resume_creator
    username: fenya
    password: 123
    driver-class-name: org.postgresql.Driver
  jpa:
    hibernate:
      ddl-auto: create-drop
  security:
    oauth2:
      client:
        registration:
          google:
            client-id: ${GOOGLE_CLIENT_ID}
            client-secret: ${GOOGLE_CLIENT_SECRET}
            scope: email, profile
            redirect-uri: http://localhost:8080/app/login/oauth2/code/google
logging:
  level:
    root: INFO
    сourse.work: DEBUG
  pattern:
    console: "%d{yyyy-MM-dd HH:mm:ss} - %level - %logger - %msg%n"